# VSCode Offline Environment Configuration

# Container image version to use
# Options: latest, 2.0.0, 2.0.0-beta, main-abc123, etc.
VSCODE_VERSION=latest

# Sync configuration
# SYNCARGS=--sync
# SYNCARGS=--syncall
# SYNCARGS=--sync --logfile /logs/sync.log

# Gallery binding
# BIND=0.0.0.0:8080

# Artifacts directory (if using custom path)
# ARTIFACTS_PATH=./artifacts/