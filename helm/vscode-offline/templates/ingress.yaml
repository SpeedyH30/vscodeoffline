{{- if and .Values.vscgallery.enabled .Values.vscgallery.ingress.enabled -}}
{{- $fullName := include "vscode-offline.fullname" . -}}
{{- $svcPort := .Values.vscgallery.service.port -}}
{{- if and .Values.vscgallery.ingress.className (not (hasKey .Values.vscgallery.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.vscgallery.ingress.annotations "kubernetes.io/ingress.class" .Values.vscgallery.ingress.className}}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-gallery
  labels:
    {{- include "vscode-offline.labels" . | nindent 4 }}
    app.kubernetes.io/component: gallery
  {{- with .Values.vscgallery.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.vscgallery.ingress.className }}
  ingressClassName: {{ .Values.vscgallery.ingress.className }}
  {{- end }}
  {{- if .Values.vscgallery.ingress.tls }}
  tls:
    {{- range .Values.vscgallery.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.vscgallery.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if .pathType }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              service:
                name: {{ $fullName }}-gallery
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}